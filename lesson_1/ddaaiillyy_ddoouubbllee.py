def crunch(string):
    string_enum = enumerate(string)
    crunched_string = ''
    for index in string_enum:
        if index(1) != 
    
    # crunched_lst = [element for element in string_lst if enumerate(string_lst)  ]

print(crunch('hhiioo'))

    

    






